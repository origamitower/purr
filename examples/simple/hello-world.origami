% version: 1

import "foo" as bar;
import "foo" exposing x, y as z;

function main(args) {
  match a {
    case Greet(who, what): who;
  }
}

class Base() {}
data class Greet(who, what) extends Base() {
  static _.hello() { who; }
  member _.whose { who; }
  member _.whose <- that { who; }
}