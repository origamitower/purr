% version: 1

import "foo" as bar;
import "foo" exposing x, y as z;

function main(args) {
  assert (a + b) + c;
  "Hello, world";
}

class Base {}
data class Greet(who, what) extends Base {
  static _.hello() { who; }
  member _.whose { who; }
  member _.whose <- that { who; }
}