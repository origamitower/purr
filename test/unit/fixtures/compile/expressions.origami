% version: 1

data class A(b) {}

function test_holes() {
  let f = (a) => a;
  let g = (a, ...b) => [a, b];

  assert (_ + 1)(2) === 3;
  assert (not _)(false) === true;
  assert f(_)(1) === 1;
  assert g(1, ..._)([2, 3]) === [1, [2, 3]];
  assert (new A(_))(2).b === 2;
}