% version: 1

data class A(b) {}

function test_holes() {
  let f = (a) => a;
  let g = (a, ...b) => [a, b];

  assert (? + 1)(2) === 3;
  assert (not ?)(false) === true;
  assert f(?)(1) === 1;
  assert g(1, ...?)([2, 3]) === [1, [2, 3]];
  assert (new A(?))(2).b === 2;
}