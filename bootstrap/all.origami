// Expressions
a |> b
a and b, a or b, ...
not a

f(a, b, c)
a.b
a."b"
a[b]

a.b()

[1, 2, 3]           // array
"foo", """foo"""    // string
true, false         // boolean

{ a, b in c }
{ c }

{ a: b }

? + ?

match expr { ... }

new Foo(a)

super(foo)
super.name(foo)

lval <- rval
