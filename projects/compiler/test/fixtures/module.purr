% language: purr

module Test.Module

import thing.foo exposing everything as x
import thing exposing x, y as z, (l + r) as (l * r)

export another.thing.here exposing everything
export thing exposing x, y as z, (l + r) as (l * r)
export thing exposing everything as x

export x, y as z, (l + r) as (l * r)