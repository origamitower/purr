% version: 1

import "foo" as bar;
import "foo" exposing x, y as z;

function main(args) {
  a.b()[c] <- d;
}

class Base() {}
data class Greet(who, what) extends Base() {
  static _.hello() { who; }
  member _.whose { who; }
  member _.whose <- that { who; }
}