% version: 1

define state(door :: Integer) -> Text
  let n = Math.sqrt(door.toFloat());
  if n === n.floor() then
    return "closed"
  else
    return "open"
  end
end

define main()
  return naturals
         |> take(100, from: ?)
         |> map(?, with: state)
         |> enumerate(?)
end