% version: 1
% pass: true

function main() {
  // Let
  let x = 1;
  let mutable y = x;

  // Assert
  assert x === 1;

  // Loop
  foreach x of y { }
  repeat while x { }
  repeat until x { }
  repeat with x from 1 to 2 { }
  repeat with x from 1 to 2 by 3 { }
  repeat { }

  // If
  if a { }
  if a { } else if b { }
  if a { } else if b { } else if c { } else if d { } else { }
  if a { } else { }
}