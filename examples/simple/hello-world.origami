% version: 1

import "foo" as bar;
import "foo" exposing x, y as z;

function main(args) {
  async (a, b) => { };
}

class Base() {}
data class Greet(who, what) extends Base() {
  member a[b] <- c { }
  member a[b] { }
  member a + b { }
  member not a { }
}